<%
     int pageNumber=1;
	 String pg = (String)request.getAttribute("page");
	     if(pg != null) {
         pageNumber = Integer.parseInt(pg);
     } 
     String nextPage = (pageNumber +1) + "";
     String myUrl = "user.do?method=userList&page=" + nextPage;
    

    
%>